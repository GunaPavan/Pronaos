# Pronaos

> Self-hosted LLM gateway with **fifty-eight empirical claims about its own behavior**, every one verified by a reproducible script or live demo.

Pronaos sits between your applications and **14 LLM providers** (Anthropic native; AWS Bedrock native with SigV4 signing for Claude / Llama / Nova / Mistral on Bedrock; **Google Vertex AI native with GCP service-account JWT auth for Gemini and Claude-on-Vertex**; OpenAI, Groq, DeepSeek, Together, Fireworks, Perplexity, xAI, Cerebras, Mistral, OpenRouter, Ollama via the OpenAI-compat adapter) behind one OpenAI-compatible API — with multi-tenant auth (**API keys plus OIDC/SSO** for human admins), cost accounting, semantic caching (now **streaming-aware** — SSE responses are captured and replayed from cache too), PII redaction (regex + Presidio ML), hash-chained audit, tool calling (full OpenAI ↔ Anthropic translation), per-provider circuit breakers (per-process by default; **Redis-backed for multi-replica convergence**), signed outbound webhooks, a pre-flight cost gate, **cost-aware auto-routing**, **quality-aware routing** (`model="auto"` picks the cheapest model that clears a measured quality bar from the team's own eval data), **request hedging** for tail-latency reduction, an **A/B testing harness** with Welch's t-test reporting statistical significance on real production traffic, and — for the IDE-class agent loop — **first-class MCP integration**: a native MCP server over SSE plus a `pronaos-mcp-proxy` stdio entry point so Claude Code / Anthropic Desktop / Cursor / Windsurf / Continue register the gateway with one `claude mcp add` line and inherit every gateway feature on every chat call. **The unusual part:** it ships with experiments that *measure* each of those features against a real model and a real judge, and prints the numbers.

---

## What this gateway can prove about itself

Fifty-eight empirical claims, each backed by a reproducible script. **Full write-ups with terminal output, methodology, and falsification conditions live in [`CLAIMS.md`](CLAIMS.md).**

| # | Claim | Headline | Reproduce |
| --- | --- | ---: | --- |
| 1 | **L1 cache faithfulness** | Δ = **0.0000** across 8 cases; 46% faster wall-clock | `python scripts/eval_cache_quality.py` |
| 2 | **Semantic cache trades nothing for paraphrase hits** | 12.5% → **87.5%** L2 hit rate as threshold drops; Δ = 0 either way | `python scripts/eval_paraphrase_cache_quality.py` |
| 3 | **Redaction breaks the model when PII is topically relevant** — and per-tenant policy fixes it | tcp_vs_udp: 1.00 → **0.00** under redaction; → **1.00** after `--disable pii.ipv4` | `python scripts/eval_guardrail_quality.py` |
| 4 | **9.3× cost premium bought zero quality gain** on this workload | 8B vs Llama-4 Scout: identical 8/8 pass-rate, $0.000050 vs $0.000463 per call | `python scripts/eval_cost_quality.py` |
| 5 | **Tamper detection works on the live audit log** | `audit verify` exits 0 on intact chain, exits 1 with exact byte diff on tamper | `pronaos-cli audit verify --tenant <id>` |
| 6 | **Circuit breaker routes around a degraded provider** | Streaming call against an OPEN breaker: **0.33 s vs 8.8 s** when CLOSED — **26.7× speedup**, zero upstream tokens consumed | [recipe in CLAIMS.md](CLAIMS.md#claim-6--circuit-breaker-routes-around-a-degraded-provider) |
| 7 | **Pre-flight token estimator saves the upstream call** on requests that would deny anyway | 1011-token estimate vs 50-token budget → **HTTP 429 with `X-Pronaos-Preflight-Estimate: 1011` header BEFORE Groq is touched** | [recipe in CLAIMS.md](CLAIMS.md#claim-7--pre-flight-token-estimator-saves-the-upstream-call) |
| 8 | **Cost-aware auto-routing cuts spend ~95% at zero quality cost** on this workload | `model="auto"` → cheapest eligible: **8/8 pass-rate held**, 77 hcents → 4 hcents (**94.8% reduction**, Δ score = 0.000) | `python scripts/eval_cost_routing.py` |
| 9 | **ML PII detection catches 9 cases regex misses entirely** — names, locations, dates, foreign-format phones | Regex-only: 0 of 11 covered cases caught. Regex+Presidio: 10 of 11 — **9 of those caught ONLY by ML.** Without Presidio, all 9 leak to the upstream. | `python scripts/eval_pii_coverage.py` |
| 10 | **Multi-judge eval reports inter-judge agreement, not just one judge's opinion** | Llama 3.3 70B + Llama 4 Scout 17B independently scored 8/8 basic-suite cases: **mean \|Δ\| = 0.000, within-ε rate = 100%** at ε=0.1 — robust agreement, not a single-judge artifact | `pronaos-cli eval run -j "groq/llama-3.3-70b-versatile,groq/meta-llama/llama-4-scout-17b-16e-instruct" ...` |
| 11 | **Quality-aware routing auto-upgrades when stored eval data shows the cheap model under-performs** | Stored score 8B=1.0, 70B=1.0 → router picks 8B (cheaper, both clear bar). Drop 8B's stored score to 0.4 → router auto-rewires to **70B**. Both decisions surfaced in `X-Pronaos-Routed-Model` + `X-Pronaos-Quality-Score` headers. | `pronaos-cli eval store-scores` + `model="auto"` ([recipe in CLAIMS.md](CLAIMS.md#claim-11--quality-aware-routing-auto-upgrades-on-stored-eval-data)) |
| 12 | **Distributed circuit breaker converges across replicas — 5× fewer wasted upstream calls** | 5 simulated replicas + 1 shared Redis: **5 cumulative failures** trip every replica in **15 ms**, vs the **25 failures** the per-process breaker would need (5 replicas × threshold 5). Atomic Lua scripts eliminate read-then-write races. | `python scripts/verify_distributed_circuit.py` |
| 13 | **OIDC/SSO admin auth works alongside API keys — no breakage to the existing path** | Real RSA-2048 keypair + JWKS served over HTTP. Gateway fetches JWKS, verifies signature, resolves `sub` → tenant, grants `admin:usage`. Unknown `sub` → 401 with the same error text as a bad API key (no enumeration leak). | `python scripts/verify_oidc_live.py` |
| 14 | **Request hedging cuts p99 by 71% at +6% upstream-call overhead** | 500-run mixed workload (7% slow-tail at 800 ms, 93% fast at 80 ms), hedge_delay = 150 ms: p99 **813 ms → 235 ms** (71.1% reduction); p95 797 ms → 219 ms (72.5%); hedge fired on **6% of calls**, hedge won **86.7%** of those races. | `python scripts/verify_hedging_latency.py` |
| 15 | **Streaming cache replay — cached SSE streams arrive 56% faster (TTFT) with zero upstream tokens** | First streaming call captures chunk + timing into Redis; second identical call replays as SSE from cache. Live against Groq 8B: TTFT **391 ms → 172 ms** (56% reduction), `X-Pronaos-Cache: hit:replay`, content matches byte-for-byte. Closes the long-standing `stream=true` cache bypass — chat apps that always stream now benefit from the cache too. | `python scripts/verify_streaming_cache_replay.py` |
| 16 | **A/B testing harness reports statistical significance on real production traffic** | Per-team `ab_test` config splits requests deterministically (hash-bucketed by `request_id` so retries land in the same arm); each call surfaces `X-Pronaos-AB-Arm` + `X-Pronaos-AB-Model` headers; `pronaos-cli abtest report` aggregates per-arm latency/cost/tokens and runs **Welch's t-test** with p-value, 95% CI, Cohen's d. Live demo: **53.8% / 46.2% split over 80 calls** (perfect bucketing within binomial noise), valid Welch's t-test result reported. Nobody in the gateway space publishes p-values on routing decisions. | `python scripts/verify_ab_test.py` |
| 17 | **Agent-turn budget gates cap a single tool-using execution before it burns the monthly budget** | Client sends `X-Pronaos-Agent-Turn-ID` on every call in one logical execution; gateway accumulates per-turn token+cost totals in Redis and 429s the call that would push the team over `agent_turn_budget_tokens`. Live demo: budget=300, 5 calls allowed (cumulative 301 tokens), call #6 denied with `agent_turn_token_budget_exhausted` + `X-Pronaos-Agent-Turn-Remaining-Tokens: 0`; fresh turn-id accepted immediately → gate is per-execution, not per-team-per-day. Closes the FinOps gap monthly budgets can't fill — a runaway agent loop. | `python scripts/verify_agent_turn_budget.py` |
| 18 | **Embedding endpoint with cache-backed zero-cost replay** — `/v1/embeddings` is a full first-class endpoint with the same auth, quota, audit, guardrails, and cache as `/v1/chat/completions`. Identical inputs hit the L1 cache and return the exact same vector with **zero upstream tokens, zero provider cost**. Five adapters: OpenAI / Mistral / OpenRouter / Cohere (`v2/embed` shape) / Voyage (`input_type` hint) plus local sentence-transformers for offline reproducibility. Live demo (local model, identical text twice): call #1 cache=miss + vector_dim=384 + prompt_tokens=7; call #2 cache=hit:exact + byte-identical vector + zero upstream invocation. RAG workloads with document re-ingestion hit ~100% cache rate. | `python scripts/verify_embeddings.py` |
| 19 | **`/v1/rerank` endpoint with cache-backed zero-cost replay** — completes the RAG triad (embed → retrieve → *rerank*). Cohere (`v2/rerank` shape with `top_n` + per-call billing) and Voyage (`v1/rerank` with `top_k` + per-token billing) adapters behind one Cohere-like public shape. Same pipeline as embeddings: auth, allowlist, preflight, ingress guardrails (on query + every document), cache, audit, usage. Live demo: 10 candidate docs + 1 query → call #1 cache=miss + cost=20 hcents + scored list with Washington D.C. on top; call #2 cache=hit:exact + byte-identical scores + zero upstream invocations (3.5× speedup at the gateway tier). No other OSS gateway caches rerank. | `python scripts/verify_rerank.py` |
| 20 | **Singleflight collapses concurrent identical requests to a single upstream call** — bursty RAG ingestion or retry-storm patterns fire N parallel identical requests on a cold cache; without singleflight all N hit the upstream. With singleflight, the first becomes the leader (does the upstream call + cache write); the rest become followers awaiting the leader's future. Standard Go-style semantics: followers see the same result (or same exception). Live demo: **50 concurrent identical /v1/embeddings on cold cache → 1 leader + 49 followers** (`pronaos_singleflight_followers_total=49`), all vectors byte-identical, 49 upstream calls eliminated. Applies to embeddings and rerank in this phase (chat is a follow-up). | `python scripts/verify_singleflight.py` |
| 21 | **Anthropic prompt-cache savings surface as response headers, response body metadata, and accurate `usage_records.cost_hcents`** — Anthropic's `cache_control` blocks give ~90% cost reduction on cached prefixes. Gateway extracts `cache_creation_input_tokens` (writes, billed 1.25x) + `cache_read_input_tokens` (reads, billed 0.10x) from both streaming and non-streaming responses; computes weighted cost; stamps `X-Pronaos-Prompt-Cache-{Read,Write}-Tokens` + `X-Pronaos-Prompt-Cache-Saved-Hcents` headers. Live demo (Claude Opus 4.7, 10k cached tokens reused on call 2): call 1 cost **19,275 hcents** (write), call 2 cost **2,025 hcents** (read) — **89.5% cost reduction** with `cache_saved_hcents=13,500`. | `python scripts/verify_anthropic_cache.py` |
| 22 | **OpenAI auto-prompt-cache savings surface through the same gateway plumbing** — OpenAI auto-caches prefixes ≥1024 tokens on supported models (gpt-4o, gpt-4o-mini, o1, gpt-4-turbo) at a 50% discount with no client opt-in. Gateway extracts `usage.prompt_tokens_details.cached_tokens` from both streaming + non-streaming OpenAI responses, normalizes `prompt_tokens` to the non-cached portion so the chat handler is provider-agnostic, applies the 0.5x discount in cost math, surfaces the same `X-Pronaos-Prompt-Cache-Read-Tokens` + `X-Pronaos-Prompt-Cache-Saved-Hcents` headers as Phase 34. Live demo (gpt-4o, 1500/2000 tokens cached on call 2): call 1 cost **550 hcents**, call 2 cost **362 hcents** → **34.2% reduction**, `cache_saved_hcents=188`. Pronaos is the only OSS gateway that correctly attributes prompt-cache savings for BOTH Anthropic AND OpenAI. | `python scripts/verify_openai_cache.py` |
| 23 | **Cross-replica singleflight collapses to one upstream call ACROSS the entire fleet** — Phase 33 ships in-memory singleflight, but a 5-replica deployment behind a load balancer still sees up to 5 concurrent upstream calls per identical-input burst. Phase 36 ships a Redis-coordinated registry (atomic SET NX claim, ~50ms polling for followers, TTL-bounded recovery) that converges dedup decisions across replicas. Live demo: **5 simulated replicas + 1 shared Redis, 50 concurrent share() calls (10 per replica) → fn ran EXACTLY ONCE globally, 1 leader + 49 followers, all results identical**. In a real 5-replica gateway: 49 upstream calls eliminated per burst (vs Phase 33's ~10/replica = 50 total). Opt-in via `PRONAOS_SINGLEFLIGHT_DISTRIBUTED=true`. | `python scripts/verify_distributed_singleflight.py` |
| 24 | **Per-tool budget caps strip exhausted tools from the upstream payload — no LLM tokens wasted on a denied tool** — Operators cap any tool name at N invocations per monthly period via `pronaos-cli team set-tool-budget`. When `current_calls >= limit_calls`, the chat handler removes that tool from the outgoing `tools` array BEFORE forwarding (strip-by-removal). The LLM never sees it, never spends reasoning on it, never wastes output tokens producing a tool_call that would be discarded. Live demo (echo_tool limit=2, 6 calls): calls 1–2 forward the tool + LLM emits it (counter → 2); calls 3–6 strip the tool + LLM produces text instead. `X-Pronaos-Tool-Stripped: echo_tool` stamped on response. `usage_records.tool_names` + `audit_records.tool_names` carry the comma-joined emitted names for FinOps and compliance queries. **No other OSS gateway has per-tool budgets**, let alone strip-by-removal. | `python scripts/verify_tool_budgets.py` |
| 25 | **Reversible PII tokenization — the upstream sees pseudonyms, the client gets originals back** — Existing redaction (Claim #3 showed) breaks topically-relevant cases by destroying the information end-to-end. Phase 38 ships the third option: ingress replaces matched PII with a deterministic, per-tenant-salted token (`[EMAIL_a3f7c2e1b890]`); the gateway holds the mapping in Redis with a per-team TTL; the egress detokenizer reverses tokens in the LLM's response before the client sees it. The upstream LLM **never** sees the original (compliance perimeter held); two mentions of the same value become the same token (entity tracking preserved); client receives real data back (UX preserved). Live demo (Groq llama-3.1-8b, email=alice@pronaos.example): `X-Pronaos-Guardrails: tokenized:email`, `X-Pronaos-PII-Reversed: 1`, client response: `'Please contact me at alice@pronaos.example.'`. Audit chain hashes the tokenized form — PII never lands on the audit log. Cloudflare AI Gateway and AWS Bedrock Guardrails do redaction; **no OSS gateway has reversible tokenization with per-tenant salt, streaming-aware reversal, and audit-chain isolation**. | `python scripts/verify_pii_tokenization.py` |
| 26 | **Gateway-side JSON Schema validation + auto-retry recovered 20% of invalid LLM responses on an adversarial prompt** — LLMs are unreliable on structured output when prompts contain instructions competing with the schema constraint ("explain first, then JSON"). Every team writes the validate+retry loop; Pronaos writes it once at the gateway. Client sends `response_format: {type: json_schema, ...}`; gateway validates the response via `jsonschema`; on violation, builds a corrective `[failed_assistant_echo, user_correction]` pair and re-fires up to `structured_output_max_retries` (default 2) times. Each retry is a real upstream call billed as its own `usage_records` row (FinOps accuracy). Live demo (Llama-3.1-8b, deliberately adversarial prompt + nested schema with SKU pattern + dual-enum + `additionalProperties: false`): **baseline 0/20 valid → with auto-retry 4/20 valid, +20pp at +25% additional upstream calls**. `X-Pronaos-Schema-Validation: passed\|retried\|failed`, `X-Pronaos-Schema-Retry-Count: N`. No other OSS gateway has gateway-side schema validation + auto-retry. | `python scripts/verify_structured_output.py` |
| 27 | **Quality regression detected with p<10⁻²⁷, traffic auto-rerouted within one check** — Closes the MLOps loop: production responses get judge-scored (per-team sampling rate); when Welch's t-test on the last N samples vs the model's stored baseline shows degradation (p<0.05), `model="auto"` excludes the degraded model from candidate selection. Hysteresis prevents flapping (detect at p<0.05, recover at p>0.10). Live demo (baseline 0.92, injected 12 samples at 0.40): `transition=detected`, `p=1.2e-27`, gateway flipped `groq/llama-3.1-8b-instant` to degraded, `model="auto"` rerouted to `groq/meta-llama/llama-4-scout-17b-16e-instruct`, `X-Pronaos-Routing-Excluded-Models` header surfaces the decision. Builds on Claim #10 (judge), Claim #11 (quality-aware routing), Claim #16 (Welch's t-test). **No OSS gateway has automated quality-regression detection wired to routing.** | `python scripts/verify_quality_regression.py` |
| 28 | **Multi-modal image input — per-tenant size cap rejects pre-flight, image-token cost surfaced on the response header** — OpenAI-shape multi-modal request (`{"type":"image_url","image_url":{"url":...}}`) lands at the gateway, gets routed to vision-capable upstreams (Groq Llama-4 Scout, Anthropic Claude vision, OpenAI gpt-4o). Anthropic adapter rewrites `image_url` → `image` block with `source.type=base64` (data URI) or `source.type=url` (HTTPS). Gateway-side `estimate_image_tokens` computes per-image cost via the right per-provider formula (gpt-4o tile algorithm vs Anthropic / Groq area formula) + stamps `X-Pronaos-Image-Tokens` + `X-Pronaos-Image-Count` headers. Per-team `max_image_bytes` (nullable, default NULL = no cap) rejects oversized payloads pre-flight with `422 image_too_large` **before any upstream call**. Live demo (Groq Scout, 64×64 PNG): 200 + `X-Pronaos-Image-Tokens: 5` + model returned `"The image is a solid light blue color."`; then with `max_image_bytes=50` the same call returned 422 with zero upstream invocations. No Pillow dependency — PNG/JPEG/GIF/WEBP headers parsed via `struct.unpack`. | `python scripts/verify_multimodal.py` |
| 29 | **Native AWS Bedrock adapter — SigV4-signed, per-model-family wire-shape translation** — Bedrock is the AWS-native procurement path for Claude / Llama / Nova / Mistral. The integration is non-trivial: auth is **SigV4-over-HTTPS** (not Bearer), the wire body shape **varies per model family** (Anthropic-on-Bedrock uses Anthropic Messages minus `model`; Llama uses a flat prompt template + `max_gen_len`; Nova uses `inferenceConfig`; Mistral uses `[INST]...[/INST]`), and the model ID lives in the URL. Pronaos ships a **true native adapter** using `botocore.auth.SigV4Auth` (byte-identical to boto3's signatures) with per-family request + response translators — NOT a thin shim over the OpenAI-compat path, and NO boto3 client dependency on the hot path (HTTP goes through gateway's existing async httpx pool so circuit breakers + hedging + OTel spans wrap Bedrock identically). **Mocked-live verify (no real AWS access required): respx-mocked endpoint exercises both Anthropic-on-Bedrock + Llama-on-Bedrock paths — Authorization header scoped to `bedrock/us-east-1/aws4_request` with a 64-hex-char signature, per-family body shapes correct, responses translate back to OpenAI-compat ChatCompletionChunk.** Substitution disclosure: respx mock, NOT real AWS — the SigV4 math, wire-shape translation, and response translation are all real, only the network hop is substituted. With real AWS creds + Bedrock model access, the same code path reaches `bedrock-runtime` successfully (32 unit + 3 chat-endpoint integration tests covering the same paths). | `python scripts/verify_bedrock.py` |
| 30 | **OTel GenAI semantic conventions compliance — Datadog / Honeycomb / Splunk GenAI dashboards work with zero field mapping** — The [OTel GenAI semantic conventions](https://opentelemetry.io/docs/specs/semconv/gen-ai/) standardise span shapes for LLM gateways. Pronaos's chat span follows the spec end-to-end: span name `chat {model}`; required `gen_ai.operation.name` + `gen_ai.system` (spec vocabulary: `aws.bedrock`, `mistral_ai`, `groq`, etc.) + `gen_ai.request.model` always present; integer-typed `gen_ai.usage.input_tokens` + `gen_ai.usage.output_tokens`; `gen_ai.response.finish_reasons` is an array (plural per spec). Backward-compatible — existing `pronaos.*` attributes still set alongside. Live verify with OTel's real `InMemorySpanExporter` (no OTLP collector required): every spec-required attribute present, type-correct. **Pronaos is the first OSS gateway to be spec-compliant with the OTel GenAI conventions out of the box** — drop Pronaos in front of a Datadog APM agent (or Honeycomb, or Splunk) and the GenAI dashboards work with zero custom field mapping. | `python scripts/verify_otel_gen_ai.py` |
| 31 | **ML jailbreak detection — Llama PromptGuard 2 catches 5 attack shapes regex misses entirely (13-case set, zero false positives)** — Phase 8.1b shipped regex prompt-injection detection. Phase 44 layers Meta's purpose-trained ML classifier (Llama PromptGuard 2 86M via Groq; forward-compatible with Llama Guard 3 / 4 hazard-category outputs S1..S14) as an async pre-check. Per-team policy with BLOCK/LOG_ONLY actions. Live demo on a curated 13-case jailbreak set: **regex caught 0, PromptGuard 2 caught 5** (direct-injection × 2, role-play × 2, suffix-injection × 1) — strict coverage extension over regex. Benign control prompt NOT falsely flagged. Same shape as Claim #9 (Presidio PII): ML catches the long-tail cases regex was never going to enumerate. Fail-open semantics — classifier outage degrades to regex-only, not gateway outage. **Pronaos is the first OSS gateway with an ML jailbreak classifier shipping with a runnable verdict script measuring the coverage delta.** | `python scripts/eval_jailbreak_coverage.py` |
| 32 | **BFCL-style tool-use accuracy benchmark — 16.7% per-model spread on a 12-case set** — Pronaos already measures answer quality (Claim #10 multi-judge, Claim #11 quality-aware routing). Phase 45 adds a different dimension via the [Berkeley Function-Calling Leaderboard](https://gorilla.cs.berkeley.edu/leaderboard.html): per-model tool-call accuracy. 12-case curated set covers simple / selection / arguments / relevance / parallel categories. Scorer: exact function-name match + AST-equivalent argument comparison (int/float coercion, key-order independent, nested dicts). Live run against 3 Groq models: **Llama 3.3 70B = 100% (12/12), Llama 3.1 8B = 91.7% (11/12), Llama 4 Scout = 83.3% (10/12)**. Per-model spread **16.7%** (threshold 10% for informative signal). Per-case failure reasons surfaced inline ("wrong_function" / "wrong_args" / "missing_call" / "unexpected_call" / "wrong_call_count") so operators see WHAT each model is bad at, not just the average. Extends Claim #11's quality-aware routing into the tool-call dimension. **First OSS gateway shipping a runnable BFCL-style benchmark with typed failure reasons.** | `python scripts/eval_tool_use_accuracy.py` |
| 33 | **Tool-use-aware routing — Phase 45's per-model accuracy composes into the router as `tool-use-aware-cheapest`** — Phase 45's BFCL signal had no routing integration; per-model scores sat in a CLI runner. Phase 46 wires them into `select_model`: when a team's strategy is `tool-use-aware-cheapest` AND the request carries tools, the scorer filters by stored tool-use accuracy BEFORE picking the cheapest survivor. Tool-less requests bypass the filter and degrade to plain `cheapest` (the filter applies surgically — when tool quality matters, never when it doesn't). Live verify against Groq: team allowlist = [70B, 8B, Scout], seed scores from Phase 45 (70B=1.0, 8B=0.917, Scout=0.833), threshold=0.95. **Request A (`model="auto"` + tools) → `groq/llama-3.3-70b-versatile`** (only model above 0.95). **Request B (`model="auto"`, no tools) → `groq/llama-3.1-8b-instant`** (cheapest in eligible pool — filter bypassed). Same opt-in semantics as Claim #11: teams that don't set the strategy see zero behavioural change. GET/PUT `/v1/admin/team/{id}/tool-use-scores` endpoints feed the column. **Closes Phase 45's "no routing integration yet" honest-limit.** | `python scripts/verify_tool_use_routing.py` |
| 34 | **Prompt-cache-aware routing — Phases 34/35's per-call cache signal composes into the router as `prompt-cache-aware-cheapest`** — Phases 34 (Anthropic) and 35 (OpenAI) surfaced per-call cache token counts on response headers + the prompt-cache metric. The data fed FinOps but not routing. Phase 47 closes the loop: a per-team `PromptCacheObserver` (Redis-backed, fail-open, 14-day sliding window) records `cached_tokens / prompt_tokens` per (team, fqmn) on every chat response. New `prompt-cache-aware-cheapest` strategy discounts each candidate's nominal input rate by `observed_hit_rate × (1 − cache_read_multiplier)` — Anthropic 0.10x, OpenAI 0.50x, others 1.0 (no-op). Live verify seeds the observer via Redis, fires `model="auto"`, asserts the new-strategy label tickets in `pronaos_routing_decisions_total`. **Discount math** (Anthropic 0.10x and OpenAI 0.50x exact-rate calculations) covered by unit tests in `test_scorer.py::TestPromptCacheAwareCostScorer`. Three admin endpoints (GET snapshot, PUT thresholds, DELETE reset). Same opt-in semantics as Claims #11 / #33: teams that don't set the strategy see zero behavioural change. **Turns the existing per-call cache measurement into a load-bearing routing input — first OSS gateway to do this.** | `python scripts/verify_prompt_cache_routing.py` |
| 35 | **Native MCP server adapter — Pronaos targetable by MCP-speaking clients (Claude Code, IDE tools) with all gateway features applied** — The Model Context Protocol is Anthropic's open spec for connecting LLMs to external systems; Claude Code, the Anthropic Workbench, and the IDE ecosystem are standardising on it. Phase 48 makes Pronaos a real MCP server at `/v1/mcp/sse`. Live verify uses the official Anthropic MCP Python SDK as a client: handshake completes (server name `pronaos` returned), `tools/list` advertises `pronaos.chat` / `pronaos.embed` / `pronaos.rerank` with well-formed JSON Schemas mirroring the REST body shapes, `tools/call` for `pronaos.chat` traverses the full MCP-to-gateway loopback path — confirmed by a `pronaos_routing_decisions_total` tick. Bearer-token auth at the SSE handshake uses the same argon2-hashed API key path as REST. Loopback HTTP forwarding from the MCP tool dispatcher preserves the entire middleware chain so MCP traffic inherits every gateway feature (auth/quotas/guardrails/cache/routing/audit) — none of which the MCP client needs to know about. Opt-in via `PRONAOS_MCP_ENABLED=true`. **First OSS LLM gateway to ship as a native MCP server.** | `python scripts/verify_mcp_server.py` |
| 36 | **Tool-call result caching — agent loops skip the client's tool re-execution when the same `(tool_name, args)` was seen before** — Composes Phase 7 (cache plumbing) + Phase 30 (agent-turn budgets) + Phase 37 (per-tool budgets) into a runtime FinOps cycle for agent loops. The gateway memoizes `(team_id, tool_name, canonical_args_json) → result` extracted from inbound `tool` role messages; on subsequent requests with trailing `assistant.tool_calls` awaiting execution, it INJECTS the cached result into the conversation before forwarding to the LLM. Live verify against Groq: call 1 populated the cache → admin GET showed the entry. Call 2 (same `(tool_name, args)`, no client-supplied result) → `X-Pronaos-Tool-Cache-Hits: 1` + LLM received the injected result. Call 3 (same tool, different args) → `X-Pronaos-Tool-Cache-Hits: 0` (canonical-args hash discriminated correctly). Per-team opt-in (`tool_result_cache_enabled`) — disabled by default; safe only for deterministic-in-args tools. Three admin endpoints (GET / PUT / DELETE). Key-order-invariant + string-vs-dict-equivalent args canonicalisation. **First OSS gateway to ship runtime-observed tool-result memoization tied to per-team policy.** | `python scripts/verify_tool_result_cache.py` |
| 37 | **MCP stdio transport — Pronaos registers with Claude Code / Anthropic Desktop / IDE MCP clients with one command** — Claim #35 made Pronaos an MCP server over SSE; Phase 50 closes the loop with the transport every IDE-class MCP client actually uses. The new `pronaos-mcp-proxy` console script is the subprocess `claude mcp add` / Anthropic Desktop / Cursor / Windsurf / Continue spawn: it loads the bearer token from `--api-key`/`--api-key-file` into the per-task ContextVar then runs the same `PronaosMcpServer` adapter from Claim #35 over stdio via `mcp.server.stdio.stdio_server`. Live verify uses the official Anthropic-maintained MCP Python SDK's `stdio_client` to spawn `pronaos-mcp-proxy.exe` as a subprocess the EXACT way Claude Code does, completes `initialize` (server name `pronaos`), discovers the three `pronaos.*` tools, and `tools/call pronaos.chat` with `model="auto"` ticks `pronaos_routing_decisions_total` by **+1** + returns real assistant content `'Hello.'` from Groq via loopback HTTP. Registration is one command — `claude mcp add pronaos -- pronaos-mcp-proxy --gateway-url http://127.0.0.1:8080 --api-key-file ~/.config/pronaos/api-key` — and every IDE chat call inherits Pronaos's full middleware chain. **First OSS LLM gateway to ship a Claude-Code-compatible stdio MCP entry point.** | `python scripts/verify_mcp_stdio.py` |
| 38 | **MCP streaming progress notifications — IDE-class MCP clients see tokens as they arrive, not after the model finishes** — Closes the documented honest-limit in BOTH Claim #35 (SSE) and Claim #37 (stdio): chat through MCP was non-streaming. When the client supplies `_meta.progressToken` on its `tools/call`, the gateway forwards with `stream=true` to its own `/v1/chat/completions`, parses every SSE chunk from the real upstream, and emits one `notifications/progress` message per chunk back through the MCP transport. The final `CallToolResult` still carries a complete non-streaming-shape ChatCompletion synthesized from accumulated deltas — clients that ignore progress notifications still see the full response. Live verify against Groq via stdio MCP (the official Anthropic SDK's `stdio_client` spawned the proxy): with progressToken, **54 progress notifications** fired during a 100-token continuation; **time-to-first-progress 1610ms, 484ms ahead** of the final result; concatenated notification messages match the final assistant text **byte-for-byte**. Without progressToken, **0** notifications fired and the non-streaming branch produced the same shape — surgically opt-in. Two new metrics (`pronaos_mcp_streaming_chunks_total{transport}`, `pronaos_mcp_streaming_sessions_total{transport, result}`) split by SSE / stdio. **First OSS gateway to ship MCP `notifications/progress` streaming through tool calls.** | `python scripts/verify_mcp_streaming.py` |
| 39 | **Bedrock streaming via AWS event-stream binary protocol — closes Phase 42's "shipped (non-streaming)" honest-limit** — Phase 42 shipped Bedrock with the documented gap that streaming used the AWS-proprietary `application/vnd.amazon.eventstream` binary protocol and would land in a follow-up phase. Phase 52 is that phase: implements a **pure-Python parser** for the binary frame format (length-prefixed prelude + headers + payload + CRC32 trailer; all 10 header value types: string/byte-array/int variants/true/false/timestamp/UUID) and wires per-family streaming-event translators for all four Bedrock families (Anthropic, Llama, Nova, Mistral). No botocore on the hot path. Live verify (mocked endpoint, **real CRC32s computed via Pronaos's own encoder**, real SigV4 math): Anthropic-on-Bedrock streamed **5 chunks** for "The quick brown fox." with terminal `finish_reason="stop"` + `prompt_tokens=18` + `completion_tokens=5`; SigV4 signature **64 hex chars** scoped to `bedrock/us-east-1/aws4_request`, Accept header `application/vnd.amazon.eventstream`, URL targeting `/invoke-with-response-stream`. Llama-on-Bedrock streamed **4 chunks** with `max_gen_len` in body, no `model` field. 26 new tests (18 parser + 8 adapter), including CRC32 mismatch detection, cross-chunk frame boundaries, and mid-stream exception-frame error paths. **First OSS gateway with a pure-Python AWS event-stream parser tied to a multi-family Bedrock streaming adapter.** | `python scripts/verify_bedrock_streaming.py` |
| 40 | **Native Vertex AI adapter — GCP service-account JWT auth + per-family wire-shape translation (Gemini + Claude-on-Vertex)** — Pronaos shipped Anthropic direct + OpenAI-compat (11 providers) + native AWS Bedrock (4 families), but Google Vertex AI was absent — GCP-hosted enterprises couldn't use the gateway at all. Phase 53 closes that gap with a third native cloud-provider integration. **Auth**: pure-Python GCP service-account JWT-bearer flow — RS256 JWT signed with `cryptography` (already a transitive dep through botocore), exchanged at oauth2.googleapis.com/token for a ~1-hour Bearer access token, cached with 5-minute leeway refresh under an asyncio.Lock. **No google-auth SDK on the hot path** — only `httpx` + `cryptography` + stdlib. **Wire shapes**: Gemini gets the native `contents`/`parts`/`generationConfig`/`systemInstruction`/`tools.functionDeclarations` (NOT OpenAI `messages`); Claude-on-Vertex gets Anthropic Messages shape with `anthropic_version="vertex-2023-10-16"` and no `model` field. **Streaming**: SSE for both — Gemini's `:streamGenerateContent?alt=sse` and Claude-on-Vertex's `:streamRawPredict`. Live verify (mocked OAuth2 + Vertex endpoints, real throwaway-RSA-2048 JWT signing that verifies against the public key): Gemini 1.5 Flash non-streaming round-tripped 12+9 tokens; Claude-on-Vertex streaming reconstructed "Jupiter is the largest planet." across 3 text chunks + terminal with correct anthropic_version. **45 new tests** (19 auth + 26 adapter). **First OSS LLM gateway with a native Vertex AI adapter using pure-Python GCP SA JWT auth.** | `python scripts/verify_vertex.py` |
| 41 | **Pronaos as MCP client — federates external MCP servers' tools into chat completions (closes the bidirectional MCP narrative)** — Phases 48–51 made Pronaos an MCP **server** (gateway exposes `pronaos.*` tools that IDE-class clients like Claude Code call). Phase 54 makes it an MCP **client**: a chat request carries `pronaos_mcp_servers: [{name, command, args, env}]`, the gateway spawns each via stdio, calls `tools/list`, prefixes each tool as `{server-name}.{tool-name}`, augments the LLM's `tools` array, and routes any tool_calls back through the right server in a bounded multi-turn loop. **Live verify**: spawned a synthesized test MCP server (in-script Python via the SDK's `Server` + `stdio_server`) exposing `get_temperature`. A chat request with `pronaos_mcp_servers=[weather → python test_weather_mcp.py]` to Groq Llama-3.3-70B → gateway discovered the tool, surfaced as `weather.get_temperature`, Groq called it with `{city: "Tokyo"}`, gateway routed the call, captured the synthetic "17 degrees Celsius" result, looped back through the upstream, **final assistant: "The current temperature in Tokyo is 17 degrees Celsius."** in **2 iterations**. Headers stamped: `X-Pronaos-MCP-Federated-Servers: weather`, `X-Pronaos-MCP-Iterations: 2`. Per-team opt-in via `mcp_client_enabled` (subprocess execution is security-sensitive). Iteration cap (default 5, max 10 via header). Per-server failure isolation. **First OSS LLM gateway with bidirectional MCP** — both a server (Phases 48–51) and a client (Phase 54). External MCP tools federate transparently, inheriting Pronaos's full middleware chain (auth/quotas/guardrails/cache/routing/audit) on every iteration. | `python scripts/verify_mcp_client.py` |

| 42 | **Anthropic prompt-cache FinOps on cloud-hosted Anthropic — Bedrock + Vertex now match direct-Anthropic FinOps surface** — Phases 34 (direct Anthropic) and 35 (OpenAI) gave each direct-API path a complete prompt-cache FinOps surface: parser extracts the cache token fields, streaming translator emits them on the terminal chunk, `cost_cents` applies the weighted multiplier (Anthropic 1.25× write / 0.10× read, OpenAI 0.50× read). But **Anthropic-on-Bedrock** and **Anthropic-on-Vertex** — where US Fortune 500 enterprises actually procure Anthropic models — dropped the same usage-block fields on the floor: naive accounting under-reported cache writes (made them look free) AND over-reported cache reads (full price instead of 10%). Phase 55 closes the gap **symmetrically** across both adapters: parser + streaming-translator + cost-math each gain Anthropic prompt-cache awareness, behind a publisher gate (`family == "anthropic"` on Bedrock, `publisher == "anthropic"` on Vertex) keeping Llama / Nova / Mistral / Gemini on plain math. **Live verify (mocked endpoints, real frame CRC32s + SSE parsing + cost math)**: Bedrock + Vertex Anthropic streams both surface `cache_creation=1000` + `cache_read=4000` on the terminal chunk; weighted cost math computes **144 hcents** on Haiku 3.5 (8 non-cached + 100 cache-write @ 1.25× + 32 cache-read @ 0.10× + 4 output), vs naive full-price's 412 hcents — a real **65% under-reporting bug** closed. Regression gates: Llama-on-Bedrock + Gemini-on-Vertex cost identical with-or-without spurious cache args. 11 new unit tests cover parser, streaming, and cost math on both adapters. **First OSS LLM gateway with weighted prompt-cache FinOps across direct Anthropic + OpenAI + Bedrock + Vertex — all four deployment surfaces.** | `python scripts/verify_anthropic_cache_cloud.py` |
| 43 | **Reasoning-token FinOps across five deployment paths — Anthropic extended thinking + OpenAI o1/o3 + DeepSeek R1 + Gemini thinking + Bedrock/Vertex Anthropic, with a Gemini cost-math correctness fix that closes a 96% per-call under-billing bug** — Reasoning models are becoming the default for hard agentic + math + code tasks, and each provider exposes "tokens-the-user-never-saw-but-the-operator-pays-for" differently. Phase 56 surfaces them uniformly across five paths: (1) Anthropic direct parses `type: "thinking"` content blocks into `reasoning_content` + estimates count via ceil(len/4); (2) OpenAI o1/o3 reads `usage.completion_tokens_details.reasoning_tokens`; (3) DeepSeek R1 reads the same field PLUS preserves `message.reasoning_content` (CoT text); (4) **Gemini thinking is the correctness fix** — `usageMetadata.thoughtsTokenCount` is a SEPARATE billable count EXCLUDED from `candidatesTokenCount`, so Pronaos ADDS it to `completion_tokens` (without this fix Pronaos was under-billing by 100% of the thinking portion — for a 20-candidate + 500-thinking request, real Google bill is 520 tokens, pre-fix Pronaos was charging the team for 20 → **96% under-charge per request**); (5) Anthropic-on-Bedrock + Anthropic-on-Vertex mirror direct Anthropic. New schema fields `chunk.reasoning_tokens` + `reasoning_content`. New `X-Pronaos-Reasoning-Tokens` response header (CoT text body-only — header intermediaries don't see it). New metric `pronaos_reasoning_tokens_total{provider, model, source}` with `source = upstream\|estimated` splitting exact (OpenAI/DeepSeek/Gemini) from inferred (Anthropic). **Live verify (parser-level, all five paths)**: Anthropic direct estimates 26 tokens, OpenAI o1 surfaces 200, DeepSeek R1 surfaces 40 + CoT text, **Gemini completion_tokens jumps 20 → 520 on the synthesized example — a 500-token under-billing gap closed**, Bedrock + Vertex Anthropic each estimate 8. Regression gate: plain Llama response leaves reasoning fields at 0/None. 11 new unit tests + 1 mocked-live verify. **First OSS LLM gateway with a unified reasoning-token FinOps surface across all major reasoning models, with a documented correctness fix that closes a real Gemini under-billing bug.** | `python scripts/verify_reasoning_tokens.py` |

| 44 | **Reasoning-aware routing — Phase 56's per-call signal composes into the router as `reasoning-aware-cheapest` with per-team safety cap** — Phase 56 surfaced reasoning tokens across five paths but the data only fed FinOps headers, not routing. Phase 57 closes the loop: a per-team `ReasoningObserver` (Redis-backed, fail-open) records `(completion_tokens, reasoning_tokens)` per (team, fqmn) on every chat call. The new `reasoning-aware-cheapest` strategy multiplies each candidate's nominal output rate by `1 + observed_reasoning_ratio` before picking the cheapest survivor — so a model with 50% observed reasoning costs 1.5× its nominal output rate in routing math. An optional per-team `max_ratio` cap **excludes** models whose observed ratio exceeds the threshold from the candidate pool entirely. Per-team thresholds (`min_samples=20` default, `max_ratio=None` default) gate noisy data; strategy degrades to plain `cheapest` when no observation has crossed the gates. Three admin endpoints (GET/PUT/DELETE) mirror Phase 47's prompt-cache shape. **Live verify (4 canonical scenarios)**: (A) no observations → degrades to cheapest; (B) realistic observations (8B at 0% + 70B at 80%) → 8B wins both plain and reasoning-aware paths (math widens the lead without flipping rank when the cheap model is already less reasoning-heavy); (C) `max_ratio=0.5` → 70B excluded entirely; (D) below min_samples → degrades to cheapest. 26 new unit tests. Same opt-in semantics as Phases 11/33/47 — teams that don't set the strategy see zero change. **Completes the routing-strategy matrix: cost / quality / tool-use / prompt-cache / reasoning — five strategies, one composable scorer scaffold.** | `python scripts/verify_reasoning_aware_routing.py` |

| 45 | **Streaming MCP federation — closes Phase 54's documented `stream=true` honest-limit** — Phase 54 (Claim #41) shipped MCP client federation but a chat with `stream=true` + `pronaos_mcp_servers` returned HTTP 422 `mcp_streaming_unsupported`. IDE-class clients (Claude Code, Cursor, Continue) that always stream couldn't use federation. Phase 58 closes the gate: the streaming wrapper reuses Phase 54's well-tested non-streaming federation loop end-to-end, then synthesizes an OpenAI-shape SSE stream from the final payload (role chunk + 64-char content chunks + terminal chunk with `finish_reason` + `[DONE]` sentinel). Federation headers (`X-Pronaos-MCP-Federated-Servers`, `-Iterations`) propagate; new `X-Pronaos-MCP-Streamed: 1` marker stamped. New counter `pronaos_mcp_streaming_federation_sessions_total{result}` splits streaming vs non-streaming sessions on dashboards. **10 new unit tests** cover SSE chunking math, role-first ordering, terminal-chunk semantics, header propagation, all three metric result labels, and that the 422 error string is fully removed from `chat.py`. **Honest-limit (documented)**: TTFT equals full federation loop latency, not first-token from the upstream — v1 synthesizes SSE from the buffered final response. Operator-runnable live verify (`scripts/verify_mcp_streaming_federation.py`) spawns a tiny test MCP server, fires a streaming chat with `pronaos_mcp_servers=[weather]`, asserts SSE wire shape + federation headers + metric tick. **Closes the documented Phase 54 honest-limit cleanly while preserving every existing federation semantic** (per-server failure isolation, iteration cap, audit/quota/guardrail middleware on each loopback call). | `python scripts/verify_mcp_streaming_federation.py` |
| 46 | **Async batches API at 50% pricing — OpenAI + Anthropic, per-team gate, polling worker, half-priced UsageRecord writes** — Both OpenAI and Anthropic ship async batches APIs at half the synchronous rate with a 24-hour completion window. Pronaos had never exposed the surface; overnight workloads paid full sync price. Phase 59 ships `POST /v1/batches` / `GET /v1/batches/{id}` / `GET /v1/batches/{id}/results` / `POST /v1/batches/{id}/cancel`, each gated on per-team `batches_enabled` (default OFF — operator opt-in). Provider routed from the first request's model with same-batch consistency (mixed-provider → 422 `batch_mixed_providers`). A new `batches` table tracks the lifecycle (`validating → in_progress → finalizing → completed | failed | expired | cancelled`); a single per-process `BatchWorker` asyncio task polls non-terminal rows every `BATCHES_POLL_INTERVAL_SECONDS` (default 60), syncs status back, and on completion fetches the result JSONL and writes one `UsageRecord` per successful sub-request with `status="batch_success"` so chargeback queries split sync vs batch spend via `WHERE status LIKE 'batch_%'`. Integer-math cost helper applies `tokens × hcents_per_mtok × 50 // (1_000_000 × 100)` from the catalog — verified mechanically: `gpt-4o-mini` at (1M prompt + 500k completion) sync=45000 hcents, batch=22500 hcents, exactly half. **Mocked-live verify**: 12/12 assertions held (submit + tick + completion + per-sub-request usage rows + half-rate math). **54 new unit tests** (33 core + 7 worker + 14 endpoint) on top of 1148 pre-existing (1202 total). Honest disclosures: mocked-live (24h wait impractical); single-worker posture (flip `BATCHES_WORKER_ENABLED=false` on N-1 replicas); v1 chat-only. **First OSS LLM gateway with a working async-batches surface across OpenAI + Anthropic with per-team policy, FinOps record-keeping, and integer-clean half-rate math.** | `python scripts/verify_batches.py` |
| 47 | **Async embedding batches at 50% pricing — closes Phase 59's chat-only honest-limit; RAG corpus ingestion at half price** — Phase 59 shipped chat batches; the documented honest-limit said "embedding batches is a future phase." Phase 60 closes that gap. RAG ingestion is the OTHER workload that burns real $ in embeddings (re-embedding millions of doc chunks on every refresh cycle), and OpenAI's batches API serves `/v1/embeddings` at the same 50% discount. Phase 60 extends the existing `POST /v1/batches` to accept `endpoint: "/v1/embeddings"`, plumbs the endpoint through `OpenAIBatchClient.submit` into the upstream's create-batch body, and routes the cost-math lookup to `entry.embedding_pricing` (separate dict from `entry.pricing` — without the discriminator the lookup silently returns 0, masking the bug). `provider_from_model` learns the `text-embedding-*` pattern; Anthropic + embeddings → 422 `embeddings_batch_unsupported_provider` (Anthropic ships no embeddings API). The worker reuses the existing parser unchanged — embedding result rows have no `completion_tokens`, the `or 0` fallback handles it. Per-sub-request `UsageRecord` rows land with `prompt_tokens > 0`, `completion_tokens = 0`, `status = "batch_success"`. **Mocked-live verify**: 14/14 assertions held — upstream POST body carries `endpoint=/v1/embeddings`, row finalises with correct counts + embedding-shaped tokens, `batch_cost_hcents("text-embedding-3-small", 1M tokens, /v1/embeddings)` = 1000 hcents (sync 2000 × 50/100), wrong-endpoint regression gate correctly returns 0. **17 new unit tests** (5 cost-math + 4 provider-routing + 3 client + 5 endpoint) on top of Phase 59's 54 batch tests + 1148 pre-existing = **1219 passing**. Honest disclosures: mocked-live; OpenAI-only by construction (Anthropic ships no embeddings API; Cohere/Voyage/Mistral ship embeddings but no batches API); 50% is OpenAI's published rate, mechanical integer-math equality verified. **First OSS LLM gateway with an async-embedding-batches surface — RAG corpus ingestion at 0.5× the per-token price end-to-end.** | `python scripts/verify_embedding_batches.py` |
| 48 | **`pronaos-cli doctor` — 14-gate operator health check distinguishes healthy from broken gateway state** — Operators discover misconfiguration today only when the first chat call returns a confusing 500 or hangs. Phase 61 ships `pronaos-cli doctor`: 14 independent gates across config (secret_key, database_url), DB (connect + migrations at head + 5 core tables), auth seed (≥ 1 tenant/team/active key), Redis ping when configured, Qdrant when semantic cache enabled, provider catalog (≥ 1 settings_attr populated), OIDC discovery, MCP SDK import, batches worker import. Each gate is `PASS` / `FAIL` / `WARN` / `SKIP`. Every gate runs even if an earlier failed — operator sees the full picture in one shot. Exit 0 on no-FAILs (WARN/SKIP allowed); `--strict` promotes WARN to FAIL for CI gating. `--probe-providers` opt-in does GET `/v1/models` per configured provider (no tokens spent). `--json` for piping into `jq`. **Mocked-live verify**: 12/12 assertions held — scenario A (healthy seeded gateway) hits 10 pass / 0 fail / 0 warn / 4 skip + exit 0; scenario B (tenant NOT seeded) hits 3 specific WARNs (tenant/team/key count = 0) + exit 0 lenient + exit 1 strict; default gate count stable across scenarios. **29 new unit tests** (4 report shape + 6 config + 4 DB + 3 auth-seed + 7 optional-backend + 2 provider-keys + 3 runner) on top of 1219 pre-existing = **1248 passing**. Honest disclosures: doctor verifies *infrastructure* is wired, not that the gateway's logic is sound; `--probe-providers` is auth-only (a 200 from /v1/models doesn't prove a real chat completion will succeed); tempfile SQLite in the verify, not Postgres. **Operator-first diagnostic — catches misconfiguration before a real chat call exposes it, ships exit codes for CI gating, prints structured JSON for piping into dashboards.** | `python scripts/verify_doctor.py` |
| 49 | **UI Foundation — Next.js 15 + TypeScript + Tailwind + shadcn/ui admin shell, full auth + dashboard against the live gateway** — Pronaos shipped 48 backend claims with **zero UI**. Operators ran ~30 CLI commands; non-technical stakeholders couldn't see anything. Phase 62 ships the foundation: Next.js 15.5.18 (App Router + RSC where appropriate) + TypeScript with `strict` + `noUncheckedIndexedAccess`, Tailwind + shadcn/ui new-york preset, light/dark theme, sonner toasts, React error boundary, full auth context (API-key bearer in localStorage), 4 shadcn primitives (Button/Card/Input/Label), top nav + collapsible side nav, `/login` + `/` (dashboard) pages, typed fetch wrapper with Zod-validated response parsing. FastAPI side adds `_mount_admin_ui` that conditionally serves `web/out/` at `/admin/*` when the build exists — one-container deployment preserved. **Backend-side verify**: 8/8 assertions held — `/v1/healthz` returns 200 + `HealthResponseSchema`; `/v1/admin/usage` returns 200 + matches `UsageResponseSchema` (`items` array + `totals` object with 5 aggregate keys + pagination); unauthenticated → 401; `/admin/` mount degrades gracefully when not built. **Browser-side verify**: 7/7 Playwright tests pass — unauthenticated→/login redirect, bad-key flow, good-key lands on dashboard with gateway version visible, sign-out clears localStorage, error states, masked session key. **Real contract bug caught**: first verify run flagged wrong endpoint name (`/v1/health` vs `/v1/healthz`) AND wrong response shape (`{rows, total_*}` vs `{items, totals, limit, offset}`) — both fixed before shipping. Honest disclosures: Phase 62 is the foundation, not the product (dashboard tiles prove connectivity, not real FinOps charts — those land in Phase 64); 10 side-nav links land on 404 today, intentionally listed to show phase-by-phase progress. **The 48 backend claims now have somewhere to render. Each subsequent phase from now on co-ships backend + UI in the same chapter.** | `python scripts/verify_ui_foundation.py` + `cd web && npm test` |
| 50 | **Identity REST + UI — tenants / teams / API keys CRUD across browser + Python, with generate-once secret invariant + full chat-key round-trip** — Pronaos's identity primitives lived only in the CLI through Phase 62; the admin UI had no way to create the keys it needed. Phase 63 closes the gap on both sides in one chapter. Backend `src/pronaos/api/v1/identity.py` adds 12 REST endpoints (tenants + teams + keys CRUD) gated on a NEW `admin:identity` scope so write power doesn't accidentally come with existing `admin:usage` keys. Generate-key returns the full secret EXACTLY once (`KeyGenerateResponse.api_key`); subsequent reads via `KeyResponse` have no `api_key` field — Pydantic enforces it, the wire literally cannot leak. Revoke is soft (`revoked_at`) so audit-chain integrity is preserved. UI ships three pages (`/tenants`, `/teams`, `/keys`) with shadcn Dialog modals, list views with create + delete confirmations, and a show-once secret modal with copy-to-clipboard + "I have saved this key" acknowledgment. **Backend verify**: 15/15 assertions held — tenant → team → key generation (full secret returned, starts with `pn_test_`), GET on the key omits the secret, the new key authenticates against /v1/chat/completions, DELETE soft-revokes, revoked key now 401s on chat, cascade deletes clean up. **Backend unit tests**: 10 new tests covering scope gate + CRUD round-trip + 422-on-bad-FK + revoke-is-idempotent + revoked-keys-cannot-auth (1258 project tests total). **UI Playwright e2e**: 4 new tests — /tenants list+create+403, /teams scoped create, /keys generate-once flow with an explicit `not.toContainText` check confirming the secret is GONE from the DOM once the show-once modal closes (the masked invariant holds at the DOM level, not just the API level). **First-impression visible product**: 4 working admin pages today vs. 0 at the end of Phase 62. | `python scripts/verify_identity.py` + `cd web && npm test` |
| 51 | **FinOps UI — spend dashboard + per-team budget editor + dialect-portable timeseries — `admin:usage` reads, `admin:identity` writes** — Pronaos persisted `usage_records` since Phase 5 and stored per-team caps since 5.7, but the only view was a CLI table — unusable for trends, impossible to share with finance. Phase 64 ships the full FinOps loop in the browser. **Backend** (`src/pronaos/api/v1/budgets.py`): `GET/PUT /v1/admin/budgets/{team_id}` for per-team caps (PUT requires `admin:identity`; partial PATCH semantics distinguish `null` = clear from omitted = unchanged via `model_fields_set`); `GET /v1/admin/usage/timeseries` returns dense zero-filled buckets capped at 1000 with bucketing done in Python so the same endpoint runs on SQLite + Postgres without dialect-specific `date_trunc` SQL. **UI**: new `/` dashboard with three summary tiles (spend / tokens / calls — last 30d) + daily-spend line chart + top-5-teams-by-spend table; `/usage` adds 24h/7d/30d window selector + team filter + time-bucketed bar chart + per-call drill-down table; `/usage/budgets` shows two progress meters (tokens + cost) per team with a Healthy/Near cap/Over cap/No cap badge + days-until-reset countdown + edit form. **Backend verify**: 21/21 assertions held — seeded 6 usage_records, asserts `/v1/admin/usage` totals match the seed ($1.50), timeseries with `bucket=day` produces dense points that re-sum to the same totals, scope split holds (admin:usage GETs 200 but PUT 403; admin:identity PUTs 200), partial PUT leaves untouched field unchanged, explicit `null` clears a cap, team_b's budget is unaffected by team_a edits. **Backend unit tests**: 11 new tests in `tests/unit/test_budgets_endpoint.py`. **UI Playwright e2e**: 4 new tests in `web/tests/e2e/finops.spec.ts` covering dashboard tiles, /usage chart+table+filter refetch, 403 error surface, and the budget edit→meter-rebind round-trip. **Closes the FinOps narrative end-to-end**: write it, read it, chart it, cap it — all from the browser with proper scope gates. | `python scripts/verify_finops.py` + `cd web && npm test` |

| 52 | **Multi-turn chat playground in the browser — SSE streaming, full response inspector, same `/v1/chat/completions` path as production** — Pronaos shipped 51 claims of gateway machinery but the only way to fire a chat through it was curl, the SDK, or the CLI — operators couldn't see what the gateway was doing per call without scraping logs. Phase 65 ships the playground: a three-column UI (parameter sidebar / conversation pane / response inspector) where every send hits the SAME `/v1/chat/completions` endpoint the SDK does, so every middleware fires on every call — auth, quotas, guardrails, cache, routing, audit. **Backend** (`src/pronaos/api/v1/models.py`): new `GET /v1/admin/models` enumerates anthropic native + every CATALOG entry annotated with `provider_configured` (mirrors `registry.available_keys()`) + `allowed` (from team's `allowed_models`); bucket-sorted (configured+allowed first, allowed-but-unconfigured next, disallowed last). **UI**: custom `streamChatCompletion()` async generator parses OpenAI-shape SSE chunks with cross-frame buffering (so a chunk split across two TCP frames still parses); response inspector reads back 7 `X-Pronaos-*` headers (Cache / Cost / Routed-Model / Routing-Strategy / Request-Id / Reasoning-Tokens / Prompt-Cache) plus client-measured TTFT + total latency; settings persist to localStorage; stream toggle off → non-streaming branch surfaces `usage` from the response body. **Backend verify**: 14/14 assertions held — models endpoint shape, anthropic native rows present, provider_configured reflects env, chat:write→403 on /admin/models, allowlist flips exactly one row's `allowed`, chat endpoint authenticates against playground keys. **Backend unit tests**: 8 new tests in `tests/unit/test_models_endpoint.py`. **UI Playwright e2e**: 4 new tests in `web/tests/e2e/playground.spec.ts` — catalog load + default-model selection, 403 surface, SSE deltas accumulating into the conversation pane with headers landing in the inspector, non-streaming branch round-trip. **First gateway-shipped admin playground that doubles as a live triage surface** — reproduce any "gateway behaving weirdly" report by firing the request from the UI and screenshotting the inspector. | `python scripts/verify_playground.py` + `cd web && npm test` |

| 53 | **Routing console — composed GET/PUT endpoint + UI for every per-team routing knob** — Phases 21–57 layered 11 per-team routing fields across the Team row (strategy + allowlist + 6 thresholds + 2 score dicts) each behind its own admin endpoint. Phase 66 ships a single composed surface: `GET /v1/admin/routing/{team_id}` returns everything in one shape; `PUT` is PATCH-style (`null` clears, omitted is unchanged via `model_fields_set`). Validation: strategy round-trips through the RoutingStrategy enum, score dicts must carry the `score` field, thresholds bounded 0..1 via Pydantic. **Scope split**: GET on `admin:usage`, PUT on `admin:identity` — routing changes are operationally sensitive. UI ships `/routing` with a team picker, 7 strategy radio cards (with explanations), a checkbox allowlist editor wired to `/v1/admin/models`, two editable score tables (quality + tool-use, preserving n_samples + source_eval_id metadata through edits), and 6 threshold inputs. **Backend verify**: 20/20 assertions held — GET shape, PATCH semantics across several round-trips, scope split (admin:usage→403 on PUT), invalid strategy/score-shape/threshold all 422, score metadata preserved, allowlist correctly distinguishes `null` vs `[]`. **Backend unit tests**: 13 new in `tests/unit/test_routing_endpoint.py`. **UI Playwright e2e**: 4 new in `web/tests/e2e/routing.spec.ts` — config load + active card, strategy click PUT round-trip, 403 surface, score inline edit. **Routing config goes from CLI/curl to one-click in the browser** with the stricter `admin:identity` scope gate enforcing the "writes are sensitive" boundary. | `python scripts/verify_routing.py` + `cd web && npm test` |

| 54 | **Security console + audit-log viewer with tamper-detection in the browser** — Phase 8/22/38/44/10 layered the security stack (regex+ML PII, reversible tokenization, ML jailbreak detection, hash-chained audit) but it was unobservable from the UI through Phase 66. Phase 67 composes a per-team `GET/PUT /v1/admin/security/{team_id}` (guardrail policy + PII tokenization in one shape) and exposes the audit log + chain verifier at `/v1/admin/audit/{tenant_id}` + `.../verify`. **Scope split**: admin:usage GETs, admin:identity writes — policy changes affect every chat-call's enforcement. UI ships `/guardrails` (team picker + rule table with action selectors + PII tokenization toggle/TTL) and `/guardrails/audit` (tenant picker + paginated records with prev_hash → this_hash linkage + "Verify chain" button surfacing pass/fail with break details). **Backend verify**: 19/19 assertions held — including the threat-model assertion: a direct SQL UPDATE to one record's `model` field flips verify to is_intact=false and surfaces the tampered record's id with reason=hash_mismatch. **Backend unit tests**: 15 new in `tests/unit/test_security_endpoint.py`. **UI Playwright e2e**: 5 new in `web/tests/e2e/security.spec.ts` — config load, PATCH wire shape (only changed field), 403 surface, verify-pass card, verify-fail card with breaks table. **First admin UI with tamper-evident audit log + per-rule policy editor end-to-end.** | `python scripts/verify_security.py` + `cd web && npm test` |

| 55 | **Reliability console + doctor in the browser — one-click circuit-breaker reset + on-demand health check** — Phase 25's per-provider circuit breakers and Phase 61's 14-gate `pronaos-cli doctor` were invisible from the UI through Phase 67. Phase 68 ships `GET /v1/admin/providers` (catalog rows + live `CircuitBreakerRegistry.snapshot()`), `POST /v1/admin/providers/{name}/reset-breaker` (force-reset to CLOSED, **admin:identity gated** since resetting a still-broken breaker re-exposes user traffic), and `GET /v1/admin/doctor` (runs the same 14 gates). UI: `/providers` table (name + configured + models + p50 latency + colour-coded circuit-state badge + per-row Reset CTA when breaker is non-closed) and `/doctor` (4 summary tiles + overall verdict banner + gate cards grouped by dotted prefix). **Backend verify (18/18 assertions held)**: providers shape + configured-first sort + tripping a real `CircuitBreaker.record_failure()` flips wire state to `"open"` + reset flips it back + reset requires admin:identity (admin:usage→403) + unknown provider → 404 + doctor summary counts always add up to the gate count + ≥10 gates always run. **Backend unit tests**: 10 new in `tests/unit/test_reliability_endpoint.py`. **UI Playwright e2e**: 5 new in `web/tests/e2e/reliability.spec.ts`. **On-call alert triage goes from "shell in + read logs" to "open `/providers`, click Reset" in ~30 seconds.** | `python scripts/verify_reliability.py` + `cd web && npm test` |

| 56 | **Batches admin console — cross-team batch list + admin cancel in the browser** — Phases 59/60 shipped async batches at 50% pricing but the only visibility was the per-team `GET /v1/batches/{id}` (chat:write scoped). Phase 69 adds three admin endpoints: `GET /v1/admin/batches` (paginated cross-team list with status/team_id filters, newest-first), `GET /v1/admin/batches/{id}` (any team's batch), and `POST /v1/admin/batches/{id}/cancel` (**admin:identity** — financially impactful to stop in-flight 50%-priced work). Invalid status string → 422. Cancel on terminal batch is idempotent. **UI**: `/batches` list with colour-coded status badges + filters + pagination + links to `/batches/[id]` detail (status card + request counts + timeline + Cancel CTA that only appears for non-terminal batches, disappears after cancel fires). **Backend verify**: 17/17 held — list/filter/pagination, status filter, team filter, invalid status 422, get + 404, scope gates (admin:usage list/get, admin:identity cancel), cancel flips in_progress → cancelled, idempotent on terminal. **Backend unit tests**: 11 new in `tests/unit/test_batches_admin_endpoint.py`. **UI Playwright e2e**: 5 new in `web/tests/e2e/batches.spec.ts`. | `python scripts/verify_batches_admin.py` + `cd web && npm test` |

| 57 | **Webhook console — cross-tenant config editor + synchronous HMAC-signed test-ping in the browser** — Phase 19 shipped webhooks but only with a tenant-isolated API (can't read/write another tenant's config). Phase 70 lifts that isolation with admin-scoped `GET/PUT /v1/admin/webhooks/{tenant_id}` (admin:usage / admin:identity) and `POST .../test` (fire a signed test event synchronously, return HTTP status). UI: tenant picker + URL+secret form + Save/Clear + test-ping card with HTTP status badge + "HMAC signed" badge + response body. **Also fixed `reuseExistingServer: false` in playwright.config.ts** — stale dev server processes were causing 6 security+webhook Playwright tests to get Next.js 404 on new routes; the fix guarantees a fresh server on every test run, and all 42 e2e tests now pass. **Backend verify**: 20/20 held — GET shape + masked secret, PUT sets + validates (invalid URL / URL-without-secret → 422), admin:usage can't write (403), clear works, test-ping fires real HMAC-signed HTTP to an in-process aiohttp receiver (http_status=200, signed=True), test-ping without config → 422. **Backend unit tests**: 13 new. **UI Playwright e2e**: 4 new. | `python scripts/verify_webhooks_admin.py` + `cd web && npm test` |

| 58 | **Settings + OIDC editor — gateway config viewer + per-tenant SSO binding — closes the Phase 62–71 UI arc** — Phase 71 is the final UI chapter. `GET /v1/admin/settings` returns a sanitised config snapshot (13 booleans + nullable strings: Redis, semantic cache, 5 providers, MCP, Presidio, singleflight, OIDC issuer, database scheme — no secrets). The identity `PATCH /v1/admin/tenants/{id}` is extended to accept `oidc_subject` (Phase 26 SSO binding was CLI-only; now settable from the browser with admin:identity scope, PATCH semantics: null/empty clears, omit preserves). **UI** `/settings`: 11 feature cards (CheckCircle/AlertCircle + enabled/disabled badge + description) + OIDC section (tenant picker + subject input + Save). **Backend verify**: 14/14 — settings shape, no secrets leaked, configured flags match env, 403 on chat:write, OIDC PATCH set/null-clear/empty-clear/preserve-on-omit all pass. **Backend unit tests**: 8 new. **UI Playwright e2e**: 3 new. **Phase 62–71 arc complete**: the admin console now covers every gateway feature — 10 phases, 16 pages, 45 Playwright e2e tests, 9 new admin REST modules. No more shell access required for day-to-day operations. | `python scripts/verify_settings.py` + `cd web && npm test` |

Most LLM-gateway documentation stops at *"the cache exists."* Pronaos closes the loop: built it → measured it → found a real failure (claim #3) → shipped per-team mitigation → re-verified the regression is gone. **The 58 claims aren't a marketing list — they're testable propositions, each falsifiable.** A future regression that breaks any of them flips a `VERDICT: claim fails` line in the corresponding script's output with a non-zero exit code, ready to wire into a CI gate. See [`CLAIMS.md`](CLAIMS.md) for the full engineering arc behind each one.

---

## See it running

`docker compose up -d && uvicorn pronaos.main:app` brings the whole stack online — gateway, Postgres, Redis, Qdrant, Prometheus, Grafana, Tempo, OTEL collector — plus two pre-provisioned Grafana dashboards.

### Visual: the FinOps dashboard during a cache demo

![FinOps dashboard showing 65.6% cache hit rate over 60 requests](docs/images/grafana-finops.png)

Captured live during `python scripts/demo_cache.py --runs 60`. The Cache hit rate panel (bottom-left) reads **65.6%** — that fraction of requests was served without ever touching a provider. Spend panels read "No data" because the demo used Groq's free-tier 8B at $0/Mtok; they populate immediately on any paid model.

> **Per-claim deep-dives** — terminal output, methodology, mitigation, falsification conditions for all 45 claims live in [`CLAIMS.md`](CLAIMS.md). The summary table above is the index; the deep-dives are the proof.

### Operational view + Swagger

![Pronaos Overview Grafana dashboard](docs/images/grafana-overview.png)

Request rate, error rate, HTTP latency p50/p95/p99, per-provider RPS + p95 latency, quota denials by reason, **guardrail hits per minute by rule** (bottom panel). The spike at ~12:50 is the cache demo run.

![Swagger UI listing /v1/healthz, /v1/chat/completions, /v1/admin/usage](docs/images/swagger-ui.png)

Auto-generated from FastAPI route definitions — try the chat endpoint at `http://localhost:8080/docs`.

---

## Feature highlights

| Area | Capability | Status |
| --- | --- | --- |
| Universal API | OpenAI-compatible `/v1/chat/completions`, streaming SSE, **`/v1/embeddings`** + **`/v1/rerank`** — both cache-backed | ✅ shipped |
| Provider support | 14 chat providers (Anthropic native + **AWS Bedrock native** with SigV4 for Claude/Llama/Nova/Mistral-on-Bedrock + **Google Vertex AI native** with GCP service-account JWT auth for Gemini and Claude-on-Vertex + 11 via OpenAI-compat) + 5 embedding providers (OpenAI / Mistral / Cohere / Voyage / local) + 2 rerank providers (Cohere / Voyage) | ✅ shipped |
| Routing & failover | Prefix-based selection; automatic retry across configured chain | ✅ shipped |
| **Circuit breaker** | Per-provider CLOSED/OPEN/HALF_OPEN; auto-skip OPEN providers; metrics + Grafana panels. **Redis-backed registry** for multi-replica convergence (Phase 25, opt-in via `PRONAOS_CIRCUIT_BREAKER_DISTRIBUTED=true`); per-process by default | ✅ shipped |
| **Tool / function calling** | OpenAI schema on input; bidirectional ↔ Anthropic translation (tool defs, `tool_choice`, `tool_use`) | ✅ shipped |
| **Streaming tools** | SSE `delta.tool_calls` accumulator (single + parallel tools, both adapters) | ✅ shipped |
| **Tool-result round-trip** | `role:"tool"` + assistant `tool_calls` echo accepted; cache correctly bypasses agent turns | ✅ shipped |
| **Streaming cancellation** | `CancelledError` propagated; `pronaos_streams_cancelled_total` metric per provider+model | ✅ shipped |
| Multi-tenancy | Tenants, teams, scoped API keys (argon2 hashing) with least-privilege scopes (`chat:write`, `admin:usage`) | ✅ shipped |
| **OIDC / SSO admin auth** | Bearer JWT accepted alongside API keys; verified against the IdP's JWKS; tenant resolved by `sub` claim. Keycloak / Auth0 / Azure AD / Google compatible. Opt-in via `PRONAOS_OIDC_ISSUER` | ✅ shipped |
| **Request hedging** | Speculative parallel start against the next chain provider after `hedge_delay_ms`; whichever finishes first wins, loser is cancelled. Cuts p99 latency without changing p50; per-team policy column + admin API. Empirically: ~70% p99 reduction at ~6% upstream-call overhead on workloads with uncorrelated slow tails | ✅ shipped |
| **A/B testing harness** | Per-team `ab_test` config splits live traffic deterministically (hash-bucketed by `request_id`); per-call `ab_arm` attribution in `usage_records`; CLI `abtest create/show/stop/report` with Welch's t-test on real production data (p-value, 95% CI, Cohen's d effect size). Surfaces `X-Pronaos-AB-*` response headers per call | ✅ shipped |
| **Agent-turn budget gates** | Per-execution token + cost cap enforced via Redis hash, keyed on the client-supplied `X-Pronaos-Agent-Turn-ID` header. Stops a runaway agent loop before it burns the monthly budget; per-turn isolation (fresh turn-id = fresh budget); response headers carry remaining budget on every call; HTTP 429 + `agent_turn_*_budget_exhausted` on cap hit; fail-open on Redis outage | ✅ shipped |
| **Embedding endpoint** | OpenAI-compatible `POST /v1/embeddings` accepting `input` as string or list, with `dimensions` (OpenAI v3.x) and `input_type` (Cohere/Voyage) pass-through. Five backends: OpenAI / Mistral / OpenRouter (OpenAI shape), Cohere (`/v2/embed` + `texts` field), Voyage (`input_type` query/document split), local sentence-transformers (zero recurring cost). Reuses chat-side cache: identical inputs return byte-identical vectors with zero upstream cost. Per-call audit row + usage record + token-budget enforcement | ✅ shipped |
| **Rerank endpoint** | `POST /v1/rerank` accepting (model, query, documents, top_n) with cache-backed zero-cost replay. Two backends: Cohere (`/v2/rerank` with `top_n` field + per-call billing) and Voyage (`/v1/rerank` with `top_k` field + per-token billing). Same pipeline as embeddings: auth, allowlist, preflight, ingress guardrails on query + every document, cache, audit, usage record. Completes the RAG triad (embed → retrieve → rerank) — no other OSS gateway caches rerank | ✅ shipped |
| **Singleflight dedup** | Process-local `SingleflightRegistry` collapses N concurrent identical requests on cold cache → 1 upstream call. The leader does the upstream + cache write; followers await the leader's future and share the result. Standard Go semantics: leader exception propagates to all followers. Applies to `/v1/embeddings` and `/v1/rerank`; chat is a follow-up. Followers stamped with `X-Pronaos-Singleflight: follower`. Metric `pronaos_singleflight_followers_total{endpoint}` measures effectiveness. **Phase 36**: opt-in cross-replica singleflight via Redis (`PRONAOS_SINGLEFLIGHT_DISTRIBUTED=true`) converges dedup across multi-replica deployments | ✅ shipped |
| **Anthropic prompt-cache FinOps surfacing (direct + Bedrock + Vertex)** | Reads `cache_creation_input_tokens` + `cache_read_input_tokens` from Anthropic's usage block on direct Anthropic, **Anthropic-on-Bedrock**, AND **Anthropic-on-Vertex** (Phase 55 closed the Bedrock + Vertex gap that under-reported the savings). Both streaming + non-streaming on all three deployment paths. Computes weighted cost: writes 1.25x input rate, reads 0.10x — matching Anthropic's published pricing, with a publisher gate so Llama / Nova / Mistral / Gemini stay on plain math. Stamps `X-Pronaos-Prompt-Cache-{Read,Write}-Tokens` + `X-Pronaos-Prompt-Cache-Saved-Hcents` response headers. Cache stats also land in `response.pronaos.cache_*` block + `usage_records.cost_hcents` is the actual (post-cache-discount) cost. Metric `pronaos_prompt_cache_tokens_total{provider, model, type}` (type=read/write) for dashboards. Pronaos is the only OSS gateway with a complete Anthropic prompt-cache FinOps surface on all three deployment paths | ✅ shipped |
| **Reasoning-token FinOps surfacing (Anthropic + OpenAI o-series + DeepSeek R1 + Gemini thinking + Bedrock/Vertex Anthropic)** | Reasoning models surface "tokens-the-user-never-saw-but-the-operator-pays-for" differently per provider: Anthropic emits `type: "thinking"` content blocks (count rolled into output_tokens — Pronaos estimates via ceil(len/4)); OpenAI o1/o3 + DeepSeek R1 expose `usage.completion_tokens_details.reasoning_tokens` (already in completion_tokens — visibility only); **Gemini exposes `usageMetadata.thoughtsTokenCount` EXCLUDED from `candidatesTokenCount`** — Phase 56 adds it to `completion_tokens` so cost math bills correctly (closes a 96% per-call under-billing bug on Gemini thinking-mode requests). New `chunk.reasoning_tokens` + `chunk.reasoning_content` fields. Response header `X-Pronaos-Reasoning-Tokens` (CoT text body-only — header intermediaries don't see it). Body field `response.pronaos.reasoning_content` carries the CoT text where Anthropic/DeepSeek ship it. Metric `pronaos_reasoning_tokens_total{provider, model, source}` with `source=upstream\|estimated` so dashboards split exact provider-reported counts from Pronaos's char-length estimate. Zero behavioural change for non-reasoning models | ✅ shipped |
| **OpenAI prompt-cache FinOps surfacing** | Reads `usage.prompt_tokens_details.cached_tokens` from OpenAI responses (both streaming + non-streaming). Normalizes `prompt_tokens` to non-cached portion so chat handler stays provider-agnostic. Applies 0.5x discount to cached tokens (OpenAI's published rate); no cache-write premium. Same response headers + body metadata + usage_records correctness as Anthropic. Other OpenAI-compat providers (Groq, DeepSeek, Together, etc.) leave the field absent — extraction falls through to 0, no behavioral change for them | ✅ shipped |
| **Multi-modal image input** | Accepts OpenAI-shape `{"type":"image_url","image_url":{"url":...}}` content parts (data URIs + HTTPS URLs both supported). OpenAI-compat path forwards verbatim; Anthropic adapter rewrites to `{"type":"image","source":{...}}` with `source.type=base64`/`url`. Gateway-side `estimate_image_tokens` computes per-image cost via the right per-provider formula (gpt-4o tile algorithm vs Anthropic / Groq area formula); surfaces `X-Pronaos-Image-Tokens` + `X-Pronaos-Image-Count` on response. Per-team `max_image_bytes` cap rejects oversized payloads pre-flight with `422 image_too_large` (zero upstream calls). PNG/JPEG/GIF/WEBP header parsing native via `struct.unpack` — no Pillow dependency | ✅ shipped |
| **Native AWS Bedrock adapter** | SigV4-signed (via `botocore.auth.SigV4Auth`, byte-identical to boto3 signatures) — NOT a shim over OpenAI-compat, and NO boto3 client dep on the hot path. Per-model-family wire-shape translators: Anthropic-on-Bedrock (Messages shape with `anthropic_version=bedrock-2023-05-31` + no `model` field), Llama (flat prompt template + `max_gen_len`), Amazon Nova (`inferenceConfig`), Mistral (`[INST]...[/INST]`). 7 catalog models across the four families with documented pricing. HTTP via httpx so circuit breakers / hedging / OTel spans / prompt-cache headers all wrap Bedrock identically to direct-API providers. **Streaming via AWS event-stream binary protocol shipped in Phase 52 (Claim #39)** — pure-Python parser, per-family streaming-event translators for all 4 families | ✅ shipped |
| **Bedrock streaming via AWS event-stream binary protocol** | Pure-Python parser for the `application/vnd.amazon.eventstream` binary frame format: length-prefixed prelude (`total_length` + `headers_length` + `prelude_crc32`), variable-length headers section (10 value types — string / byte-array / int8/16/32/64 / true / false / timestamp / UUID), payload, message CRC32 trailer. No botocore dep on the hot path (stdlib `binascii.crc32` + `struct.unpack` only). Per-family streaming-event translators emit canonical `ChatCompletionChunk` instances: Anthropic-on-Bedrock (`message_start` → `content_block_delta` → `message_stop` event sequence, tool_use accumulation across `input_json_delta` frames), Llama (per-frame `generation`), Nova (`contentBlockDelta` + `messageStop` + `metadata`), Mistral (per-frame `outputs[0].text`). Mid-stream `:message-type=exception` frames translate to `ProviderError(retryable=True)` so failover sees them as 502s. CRC32 mismatches raise non-retryable errors — silent corruption never reaches the consumer | ✅ shipped |
| **Native Vertex AI adapter (GCP)** | Pure-Python GCP service-account JWT-bearer flow — operator drops the SA JSON (or points `GOOGLE_APPLICATION_CREDENTIALS` at it), gateway signs an RS256 JWT via `cryptography` (already a transitive dep through botocore), exchanges at `oauth2.googleapis.com/token` for a Bearer access token, caches with 5-minute leeway refresh under an `asyncio.Lock`. **No `google-auth` SDK on the hot path** — just `httpx` + `cryptography` + stdlib. Per-publisher wire-shape translators: Gemini (`contents`/`parts`/`generationConfig`/`systemInstruction`/`tools.functionDeclarations`), Claude-on-Vertex (Anthropic Messages with `anthropic_version="vertex-2023-10-16"` and no `model` field). Streaming via SSE for both — Gemini uses `:streamGenerateContent?alt=sse`, Claude-on-Vertex uses `:streamRawPredict`. Six catalog models across the two publishers (Gemini 1.5 Flash/Pro, Gemini 2.0 Flash, Gemini 2.5 Pro, Claude 3.5 Haiku/Sonnet on Vertex) with documented pricing. HTTP via httpx so circuit breakers / hedging / OTel spans / prompt-cache headers all wrap Vertex identically to direct-API providers | ✅ shipped |
| Rate limits | Per-key RPS token bucket — in-memory (dev) / Redis Lua (prod) | ✅ shipped |
| Token + cost budgets | Per-team monthly limits with calendar-month rollover, atomic SQL writes | ✅ shipped |
| **Pre-flight quota gate** | Heuristic token estimator denies over-budget requests BEFORE the upstream call (saves real cost) | ✅ shipped |
| **Cost-aware auto-routing** | `model="auto"` resolves to cheapest / fastest / balanced eligible model per team policy; capability-filtered (tools, vision, context) | ✅ shipped |
| **Quality-aware routing** | `quality-aware-cheapest` strategy filters by per-model eval scores from the team's own workload before picking the cheapest; `pronaos-cli eval store-scores` bridges the eval harness to the router | ✅ shipped |
| **Tool-use-aware routing** | `tool-use-aware-cheapest` strategy filters by per-model BFCL-style tool-call accuracy (Phase 45 signal) BEFORE picking the cheapest — only when the request carries tools. Tool-less requests bypass the filter and degrade to plain `cheapest`. Per-team threshold + scores written via PUT `/v1/admin/team/{id}/tool-use-scores`; same opt-in semantics as quality-aware routing | ✅ shipped |
| **Prompt-cache-aware routing** | `prompt-cache-aware-cheapest` strategy discounts each candidate's input rate by the team's runtime-observed cache hit rate × the provider's cache-read pricing multiplier (Anthropic 0.10x, OpenAI 0.50x, others 1.0 = no-op). Per-team Redis-backed `PromptCacheObserver` records observations from every chat response; the scorer reads the snapshot at routing time. Per-team thresholds (`min_samples`, `min_hit_rate`) gate noisy data. GET snapshot, PUT thresholds, DELETE reset endpoints. Same opt-in semantics as Claims #11 / #33 | ✅ shipped |
| **Reasoning-aware routing** | `reasoning-aware-cheapest` strategy multiplies each candidate's output rate by `1 + observed_reasoning_ratio` (so a model with 50% reasoning costs 1.5× nominal output). Per-team Redis-backed `ReasoningObserver` (mirrors PromptCacheObserver) records `(completion_tokens, reasoning_tokens)` per (team, fqmn) on every chat call from Phase 56's surfacing. Optional `max_ratio` cap **excludes** models whose observed ratio exceeds the threshold from the candidate pool entirely. Per-team thresholds (`min_samples=20`, `max_ratio=None` defaults) gate noisy data. Degrades to plain cheapest when no observation has crossed the gates. GET snapshot, PUT config, DELETE reset endpoints. Completes the routing-strategy matrix: cost / quality / tool-use / prompt-cache / reasoning — five strategies, one composable scorer scaffold | ✅ shipped |
| **Native MCP server** | Pronaos exposes `pronaos.chat` / `pronaos.embed` / `pronaos.rerank` as MCP (Model Context Protocol) tools at `/v1/mcp/sse`. Any MCP-speaking client (Claude Code, Anthropic Workbench, IDE integrations, MCP-aware agents) can connect with the team's existing Pronaos API key as a Bearer token; the SSE transport hands off to the official Anthropic MCP SDK's server; `tools/call` forwards via loopback HTTP through the full middleware chain so MCP traffic inherits every gateway feature (auth, quotas, guardrails, cache, routing, audit, prompt-cache surfacing) automatically. Opt-in via `PRONAOS_MCP_ENABLED=true` | ✅ shipped |
| **MCP stdio transport — Claude Code / IDE registration** | `pronaos-mcp-proxy` console script registered via `pyproject.toml [project.scripts]` IS the subprocess that IDE-class MCP clients spawn. Parses `--gateway-url` + `--api-key`/`--api-key-file`, runs the same `PronaosMcpServer` adapter from the SSE transport over stdio via `mcp.server.stdio.stdio_server`. Registers in one line: `claude mcp add pronaos -- pronaos-mcp-proxy --gateway-url http://127.0.0.1:8080 --api-key-file ~/.config/pronaos/api-key`. Bearer token loaded once into a per-task ContextVar; tool dispatcher reuses Claim #35's loopback-HTTP forwarding so every IDE chat call traverses the full middleware chain. SDK shape: `StdioServerParameters(command="pronaos-mcp-proxy", args=[...])` — exactly what Claude Code, Anthropic Desktop, Cursor, Windsurf, and Continue construct. Live-verified against the official Anthropic MCP Python SDK's `stdio_client` spawning the proxy as a real subprocess (Claim #37) | ✅ shipped |
| **MCP streaming progress notifications** | When the MCP client supplies `_meta.progressToken` on its `tools/call` for `pronaos.chat`, the gateway forwards with `stream=true` to its own `/v1/chat/completions`, parses every SSE chunk from the real upstream as it arrives, and emits one `notifications/progress` message per chunk back through the MCP transport (sse OR stdio). The final `CallToolResult` still carries a complete non-streaming-shape ChatCompletion synthesized from accumulated deltas, with a `pronaos.mcp_streamed=true` marker — clients that ignore progress notifications still see the full response. Per-transport metrics: `pronaos_mcp_streaming_chunks_total{transport}` + `pronaos_mcp_streaming_sessions_total{transport, result}`. Streaming is surgically opt-in: zero progressToken → zero notifications, identical to today's non-streaming behaviour. Closes the documented honest-limit in both Claim #35 and Claim #37 (Claim #38) | ✅ shipped |
| **MCP client federation — Pronaos as an MCP client** | Closes the bidirectional MCP narrative. A chat request can carry `pronaos_mcp_servers: [{name, command, args, env}]`; the gateway spawns each via stdio, calls `tools/list`, prefixes each tool as `{name}.{tool}` to avoid collisions, augments the LLM's `tools` array, and routes any tool_calls back through the right server in a bounded multi-turn loop (default 5 iterations, max 10 via `X-Pronaos-MCP-Max-Iterations` header). Per-team opt-in via `team.mcp_client_enabled` (subprocess execution is security-sensitive). Per-server failure isolation — one broken server doesn't fail the chat; failed servers surface via `X-Pronaos-MCP-Failed-Servers` header. Federation lives inside the chat handler so the loopback HTTP loop inherits the full middleware chain (auth/quotas/guardrails/cache/routing/audit) on every iteration. Metrics: `pronaos_mcp_federated_tool_calls_total{server,tool,result}` + `pronaos_mcp_federation_sessions_total{result}`. Live-verified end-to-end: a Groq Llama-3.3-70B chat called a synthesized test MCP server's `get_temperature` tool, the gateway routed → got the result → looped → final assistant response carried the right answer (Claim #41) | ✅ shipped |
| **Streaming MCP federation (Phase 58)** | Phase 54 shipped MCP federation but a `stream=true` + `pronaos_mcp_servers` chat returned HTTP 422 `mcp_streaming_unsupported` — IDE-class clients (Claude Code, Cursor, Continue) that always stream couldn't use federation. Phase 58 closes the gate: the streaming wrapper reuses Phase 54's well-tested non-streaming federation loop end-to-end and synthesizes an OpenAI-shape SSE stream from the final payload (role-first chunk + 64-char content chunks + terminal chunk with `finish_reason` + `[DONE]` sentinel). Federation telemetry headers propagate from the inner loop's Response onto the StreamingResponse, plus a new `X-Pronaos-MCP-Streamed: 1` marker. New counter `pronaos_mcp_streaming_federation_sessions_total{result}` ticks alongside the existing `mcp_federation_sessions_total` so dashboards can split streaming vs non-streaming sessions. Honest-limit (documented): TTFT equals full federation loop latency, not first-token from the upstream — v1 synthesizes SSE from the buffered final response. **Closes the documented Phase 54 honest-limit; the request shape `{stream: true, pronaos_mcp_servers: [...]}` is now accepted end-to-end** | ✅ shipped |
| **Tool-call result caching** | The gateway memoizes `(team_id, tool_name, canonical_args_json) → result` extracted from inbound `tool` role messages; on subsequent requests with trailing `assistant.tool_calls` awaiting execution, the cached result is INJECTED into the conversation before forwarding to the LLM — skipping the client's tool re-execution round trip. Per-team opt-in (`tool_result_cache_enabled`), per-team TTL. `X-Pronaos-Tool-Cache-Hits` / `X-Pronaos-Tool-Cache-Tools` response headers + `pronaos_tool_result_cache_total{tool_name, result}` metric. Safe only for deterministic-in-args tools — operator owns the policy | ✅ shipped |
| **Per-team model allowlist** | fnmatch patterns; NULL = unrestricted, `[]` = paused-deny-all; CLI + admin API | ✅ shipped |
| Cost accounting | Per-call audit rows, `GET /v1/admin/usage` with filters, `team chargeback` CLI | ✅ shipped |
| Prometheus + Grafana | `/metrics` endpoint, two provisioned dashboards (11 panels), OTEL → Tempo | ✅ shipped |
| OpenTelemetry | FastAPI / httpx / SQLAlchemy + named spans for `pronaos.quota.check` and `pronaos.provider.call` | ✅ shipped |
| **OTel GenAI semantic conventions** | Every chat span carries the standard [`gen_ai.*` attributes](https://opentelemetry.io/docs/specs/semconv/gen-ai/) — `gen_ai.operation.name`, `gen_ai.system` (spec vocabulary: `aws.bedrock`, `mistral_ai`, `groq`, etc.), `gen_ai.request.model`, `gen_ai.usage.input_tokens` + `output_tokens` (integer-strict), `gen_ai.response.finish_reasons` (array-typed, plural per spec), `gen_ai.response.id` + `.response.model`. Span name follows `{operation} {model}`. Backward-compatible — existing `pronaos.*` attributes still set alongside. **Datadog / Honeycomb / Splunk GenAI dashboards work with zero custom field mapping.** | ✅ shipped |
| **Outbound webhooks** | HMAC-SHA256-signed POST for `quota.exhausted` / `circuit.tripped` / `audit.chain_broken`; retry-on-5xx | ✅ shipped |
| Semantic caching | Two-tier (Redis exact-match + Qdrant embedding-based), tenant-isolated by construction. **Streaming-aware** (Phase 28): captures chunk + inter-chunk timing on the first SSE call, replays from cache on hits. Closes the long-standing `stream=true` bypass | ✅ shipped |
| Guardrails | PII redaction (5 rules + Luhn) + prompt-injection detection, ingress + egress + streaming-aware | ✅ shipped |
| **ML PII detection** | Microsoft Presidio (spaCy NER) catches names / locations / dates / foreign formats regex misses; per-entity rule names (`presidio.PERSON`, `presidio.LOCATION`, ...); opt-in via `PRONAOS_PRESIDIO_ENABLED` | ✅ shipped |
| **ML jailbreak / prompt-injection detector** | Llama PromptGuard 2 86M via Groq as an async pre-check in front of the regex/Presidio stack. Per-team policy with BLOCK/LOG_ONLY actions. Forward-compatible with Llama Guard 3 / 4 hazard-category outputs (S1..S14). Fail-open on classifier outage — gateway keeps serving with regex + Presidio still active. Opt-in via `PRONAOS_LLAMA_GUARD_ENABLED` + per-team `guardrail_policy.llama_guard.enabled`. Empirical claim #31: caught 5 jailbreak shapes regex missed on a 13-case set, zero false positives on benign control | ✅ shipped |
| Per-team policy | `Team.guardrail_policy` lets ops disable specific rules per tenant; toggle Presidio per-team; admin API + CLI | ✅ shipped |
| Eval harness | LLM-as-judge scorer, YAML golden sets, CLI runner, **six bundled experiments** | ✅ shipped |
| **Multi-judge eval** | N concurrent judges per case + pairwise agreement (mean Δ, within-ε rate, Cohen's κ); robust scoring not single-judge bias | ✅ shipped |
| **BFCL tool-use benchmark** | Berkeley Function-Calling Leaderboard-style scorer + 12-case curated golden set (simple / selection / arguments / relevance / parallel). Exact function-name match + AST-equivalent argument comparison (int/float coercion, key-order independent, nested dicts recursive). Per-case typed failure reasons ("wrong_function" / "wrong_args" / "missing_call" / "unexpected_call" / "wrong_call_count"). Live runner aggregates per-model + per-category accuracy; verdict reports the per-model spread. First OSS gateway shipping a runnable BFCL-style benchmark | ✅ shipped |
| Audit log | Per-tenant hash-chained record; `pronaos-cli audit verify` walks the chain | ✅ shipped |
| Admin CLI | `pronaos-cli` for tenant / team / key / budget / policy / allowlist / webhook / audit | ✅ shipped |
| Admin UI | Next.js 15.5 app — 17 pages across identity, FinOps, playground, routing, guardrails, reliability, batches, webhooks, and settings; 51 Playwright e2e tests | ✅ shipped |
| Deploy | Helm chart + Terraform module for one-command production install | 🔜 roadmap |

---

## Architecture

```
client app ─► Pronaos ─► [ auth ─► allowlist ─► preflight ─► guardrails ─► cache ─► failover ─► provider ]
                │                                                              │              │
                │                                          (per-provider       │              │
                │                                           circuit breaker)   │              │
                ├─► OTEL collector ─► Tempo / Prometheus / Grafana             │              ├─► Anthropic
                ├─► Postgres (tenants, keys, quotas, usage, audit, policy)     │              ├─► AWS Bedrock (SigV4 native: Claude / Llama / Nova / Mistral)
                ├─► Redis + Qdrant (L1 cache + L2 semantic cache, rate limits) │              └─► Groq / OpenAI / DeepSeek / Together / Fireworks / Perplexity / xAI / Cerebras / Mistral / OpenRouter / Ollama (via OpenAI-compat adapter)
                └─► Webhook dispatcher ─► tenant's incident channel (Slack / PagerDuty / …)
```

Every request, in order: auth → model-allowlist gate → pre-flight token estimate → ingress guardrails → cache lookup → routing → circuit-breaker-aware failover → provider call → egress guardrails → cache write → audit append → usage record → observability export. Streaming responses get the same coverage (egress + audit run at stream close; cancellation propagates cleanly to upstream).

Operationally-significant events (quota exhaustion, circuit trips, audit chain breaks) push out as HMAC-signed webhook deliveries to the tenant's configured receiver.

Deeper deep-dive in [`ARCHITECTURE.md`](ARCHITECTURE.md).

---

## Quickstart

Prerequisites: Python 3.12. Docker is optional (only for the full observability stack).

```bash
git clone https://github.com/GunaPavan/Pronaos.git
cd Pronaos
cp .env.example .env       # or `Copy-Item .env.example .env` on Windows

# Cross-platform task runner (Windows: tasks.cmd / macOS+Linux: make):
./tasks.cmd install        # creates .venv and installs deps   (or: make install)
./tasks.cmd db-upgrade     # apply Alembic migrations          (or: make db-upgrade)
./tasks.cmd dev            # start the gateway on :8080        (or: make dev)
./tasks.cmd test           # 1352 pytest tests + 51 Playwright e2e
```

Smoke test: `curl -s http://localhost:8080/v1/healthz` → `{"status":"ok","version":"0.1.0"}`.

### Full observability stack

```bash
docker compose up -d   # Postgres, Redis, Qdrant, Prometheus, Grafana, Tempo, OTEL collector
```

Open **http://localhost:3000** → Dashboards → **Pronaos**. Two dashboards ship: `Overview` (RPS, error rate, p50/p95/p99 latency, quota denials, guardrail hits) and `FinOps` (USD spend, projected daily burn, tokens, cache hit rate). Details in [`observability/README.md`](observability/README.md).

### Run the demos

Mint an API key once (`pronaos-cli key issue --team <team-id>`), then:

```bash
python scripts/demo_cache.py --api-key pn_live_... --runs 60          # cache effectiveness
python scripts/demo_guardrails.py --api-key pn_live_...               # PII redaction + injection
pronaos-cli eval run -g tests/eval/data/basic.yaml \
    -c groq/llama-3.1-8b-instant -j groq/llama-3.3-70b-versatile \
    -k pn_live_...                                                     # eval harness
```

Then the four experiments backing claims #1–#4:

```bash
python scripts/eval_cache_quality.py          --api-key pn_live_...   # claim #1
python scripts/eval_paraphrase_cache_quality.py --api-key pn_live_...   # claim #2
python scripts/eval_guardrail_quality.py      --api-key pn_live_...   # claim #3
python scripts/eval_cost_quality.py           --api-key pn_live_...   # claim #4
```

Full details in [`scripts/README.md`](scripts/README.md).

---

## What's left

Active roadmap items (everything else in the Feature highlights table is shipped):

- **Cross-replica HALF_OPEN single-probe lock** — Phase 25 ships shared trip state. Atomic single-probe coordination across replicas (only one replica sends the HALF_OPEN probe, others wait) is a follow-up; today every replica is allowed to probe and the first-to-record wins
- **Anthropic live verification of streaming tool_use** — unit-tested with realistic SSE bodies; needs a real Anthropic key for full end-to-end demo
- **Per-team Presidio entity / min_score overrides** — policy validator accepts the keys today, but only the `enabled` shorthand is honoured at request time. Per-entity disabling and per-team thresholds are a small follow-up
- **Periodic auto-eval-then-store** — Phase 24 ships the data plumbing for quality-aware routing; an opt-in scheduler that re-runs the eval on a cadence and refreshes `quality_scores` is a polish step
- **Helm + Terraform** — one-command production deploy
- **SCIM provisioning** — Phase 26 ships JWT-based OIDC admin auth. SCIM (the protocol for auto-provisioning users from an IdP) is the next layer for IT-team workflows; not yet shipped

See [`ARCHITECTURE.md`](ARCHITECTURE.md) for the full system shape and the "what's not built" section.

---

## License

MIT — see [`LICENSE`](LICENSE).
